# {{projectName}}

ANA scrolling starter project. This is an A1200-baseline-oriented skeleton that
declares side-scroll intent without using future AGA-only features.

This project includes a local `.ana-sdk` copied by ANA for VS Code. Run:

```sh
make
make adfs
```
